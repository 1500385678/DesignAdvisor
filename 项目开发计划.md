# 27-设计-Design Level · 产品立项与技术方案

> 项目代号: DesignAdvisor · 内部代号 27-设计 · v1.0 · 2026-08-23
> 最近更新: 2026-09-10(T1 0910 ← **Phase 0 #4 飞书 bot 增强 - 切真发 smoke 工具**:新增 `bot/live_send.py` 切真发入口(`LiveSendConfig` dataclass + `probe_lark_cli()` 调 `lark-cli --profile <p> im +messages-send --help` 做无副作用鉴权校验 + `send_via_lark()` 先 probe 后 send 编排 + `ProbeError` 异常 + `from_env()` 工厂 + `__main__` CLI 调试入口,4 env 变量:`FEISHU_BOT_DRY_RUN` 默认 1 / `LARK_CLI_BIN` 默认 lark-cli / `LARK_PROFILE` 默认 design / `FEISHU_BOT_DEFAULT_CHAT_ID` 默认 None)+ `bot/test_live_send.py` 8 单元 + `bot/__init__.py` v0.3.0 → v0.4.0 增列 live_send 子模块 + README 新增"切真发部署清单"小节(5 步部署:配 lark-cli profile + 填 .env + probe + send_via_lark 单条真发 + 5 设计师 dogfood),目录结构段补 live_send.py / test_live_send.py;8/8 单元 + 3/3 端到端验证通过(8 单元:env 默认值 + env chat_id 覆盖 + dry_run=True 不调 subprocess + dry_run=False 调 subprocess 正确 + chat_id 缺失 raise ValueError + 显式 chat_id 覆盖 default + probe 调 lark-cli --help + probe 失败 raise ProbeError;3 端到端:probe 缺 lark-cli 抛 ProbeError + dry_run 模式 print 命令 probe_ok=False + chat_id 缺失退出码 2);`bot/` 从 1340 行 → ~1540 行(+200 行,新 live_send.py 165 行 + test_live_send.py 200 行 + __init__.py +3 行);**飞书 bot 增强 4/4 卡片化 + URL 验签 + 限流 + 切真发 smoke 工具全部闭环**,试运行前 4 件套全栈就绪;**下一刀候选**:5 设计师 dogfood 验收(关闭 dry_run 后首日 dogfood,纯外部沟通)+ Phase 1 #1 Figma OAuth 真实入库(纯外部沟通);Phase 1 #1 4/5 不变,仍剩 Figma OAuth 真实入库最后 1 子项)
> T4/T5 commit 简注:
> - T4 0828 ← `bc5822e` · 24 条 DP 入 API 库
> - T5 0829 ← `841f251` · Web 端 DP 浏览页 SSR v0.3
> - T5 0831 ← `fbf7405` · Phase 0 #2 可入库资产清单 v0.1 落地
> - T5 0901 ← `c8b4a7b` · Phase 1 #1 资产库 MVP 后端 fake-load 端点 v0.3
> - T5 0902 ← `060e5d5` · Phase 1 #1 资产库 MVP 前端列表页 v0.4
> - T1 0903 ← `3d7d214` · Phase 1 #1 资产库 MVP 全量回填 133 件 v0.3
> - T1 0904 ← `73f31b7` · Phase 1 #1 资产库 MVP 语义搜索端点 + 前端搜索框 v0.4 / v0.5
> - T1 0905 ← `76acca8` · **Phase 0 #4 飞书 bot 雏形闭环** + 后端 v0.3 → v0.4
> - T1 0906 ← `225fd70` · **Phase 0 #4 飞书 bot 增强 - 卡片化** + `bot/card.py` 飞书交互卡片渲染器
> - T1 0908 ← `63f31c5` · **Phase 0 #4 飞书 bot 增强 - URL 验签** + `bot/signature.py` HMAC-SHA256 验签
> - T1 0909 ← `见本日 commit` · **Phase 0 #4 飞书 bot 增强 - 限流** + `bot/ratelimit.py` token bucket 限流
> - T1 0910 ← `见本日 commit` · **Phase 0 #4 飞书 bot 增强 - 切真发 smoke 工具** + `bot/live_send.py` 切真发入口 + probe_lark_cli 鉴权探针

## 0. 文档地图

- **本文(T1 主计划 · 执行态)**:聚焦设计资产库 / Figma 同步 / 评审协作 / AI 草稿 / 飞书 bot,Phase 0-3 落地路径。
- **[[docs/01-设计顾问-技术方案-v1.0]]**:v1.0 技术方案存档,覆盖愿景/形态/五大模块/技术栈/SQLite→PG 迁移/Phase 0-4 计划,与本文互为补充。
- **[[docs/02-设计资产元数据-schema]]**:Phase 0 #5 元数据 schema v0.1,资产库底层定义。
- **[[docs/03-_DesignLib盘点_设计哲学清单_v0.1]]**:Phase 0 #1 `_DesignLib` 10 章盘点,24 条 DP / 9 条大师名言 / 3 条悖论。
- **[[docs/04-可入库资产清单_v0.1]]**:Phase 0 #2 4 类资产骨架(组件 32 / 页面 19 / 令牌 ~73 / 参考开放),对齐 `02-schema` v0.1,Phase 1 OAuth 后遍历入库。

> T1 主计划聚焦"资产库+评审",v1.0 方案侧重"知识图谱+案例+AI 评估",两者长期会融合为同一 Web 平台(参见 01-的 Phase 1-2 与本文 Phase 1 的"AI 草稿生成器"对齐点)。

---

## 1. 项目概述

**愿景**:把"设计资产管理"从文件夹/Sketch 库/Figma 链接散落状态,整合为可被 AI 调用、可被设计师搜索、可被产品/研发引用的统一设计中枢。设计不只是"产出图",更是被版本化、被评审、被复用的资产。

**形态**:Web App(主) + 飞书 Agent(辅)
- Web App 形态:设计资产库(组件/页面/设计令牌/参考)、设计评审协作、AI 辅助设计建议
- 飞书 Agent 形态:在飞书群@bot 可查询"我们公司按钮的设计规范"、"找某个风格的登录页参考"等
- 与 Figma 打通:同步 Figma 组件库版本、获取设计源文件,做"代码-设计"双向追溯

**为什么是这个形态**:设计资产的体量大、需要图形化检索/预览,Web 是必然;但设计师的协同入口在飞书,Agent 形态解决"随手问"场景。

## 2. 产品定位

**目标用户**(3-4 群):
1. **UI/UX 设计师**:日常查组件库、找参考、版本管理
2. **产品经理**:在写 PRD 时需要引用设计规范、找参考页面
3. **前端工程师**:实现设计稿时需要精确的设计令牌(颜色/间距/字号)与代码片段
4. **设计总监/品牌负责人**:管理品牌一致性、设计规范演进

**核心价值**(3-4 点):
- **资产可搜索**:用自然语言找"登录页带主视觉的版本",不用翻文件夹
- **设计-代码一致**:设计令牌与前端 token 双向同步,避免实现偏移
- **评审留痕**:每个设计决策留下评审记录(谁批/为何改/历史版本对比)
- **AI 辅助**:不替代设计师画图,但能基于现有规范生成"草稿",减少从 0 起步

**差异化**:不与 Figma/Sketch 抢"画图"主战场(那是设计师的创作工具);只做"画完之后"的资产管理、版本、评审、AI 增强,与 Figma 形成上下游。

## 3. 核心功能

**模块 1:设计资产库**
按层级组织(品牌 → 设计令牌 → 组件 → 页面 → 模板),每件资产带元数据(版本/作者/用途/历史/关联代码),支持语义搜索(自然语言 + 标签 + 视觉相似度)。

**模块 2:设计令牌同步中心**
维护设计令牌(颜色/字体/间距/阴影)的 single source of truth,与 Figma Tokens 插件 + 前端 Style Dictionary 双向同步,自动检测偏移并告警。

**模块 3:设计评审协作**
设计师上传稿子 → 系统自动@评审人(产品/研发/品牌)→ 评审意见结构化(通过/修改/拒绝 + 理由)→ 决策可追溯;支持版本对比可视化。

**模块 4:AI 草稿生成器**
基于现有设计规范 + 用户输入需求,生成设计草稿(Figma JSON / Sketch 文件 / HTML+CSS);明确"草稿"定位,设计师必须修改后才能定稿,避免直接用 AI 出图。

**模块 5:设计灵感库(用户贡献)**
设计师看到的优质外部参考可一键收藏入库,带来源标注;内部共享,积累团队审美资产。

## 4. 技术架构

```
┌─────────────────────────────────────────────────────┐
│  Web App (Next.js + Tailwind + Radix UI)            │
│  - 资产浏览 · 搜索 · 预览 · 评审 · 导出              │
│  - 飞书 Bot:对话式查询                               │
└────────────────────┬────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────┐
│  API 网关 (FastAPI)                                  │
└────────────────────┬────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────┐
│  核心服务层                                          │
│  - 资产索引服务 (Elasticsearch + 向量)               │
│  - 设计令牌服务 (双向同步引擎)                        │
│  - 评审工作流服务 (BPM 引擎)                         │
│  - AI 草稿生成器 (LLM + 模板渲染)                    │
│  - 灵感库 (用户贡献 + 标签提取)                       │
└────────────────────┬────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────┐
│  数据层                                              │
│  - 资产元数据 (PostgreSQL)                           │
│  - 文件存储 (S3 兼容, 设计源文件)                    │
│  - 设计令牌历史 (时序 DB)                            │
│  - 评审记录 (PostgreSQL)                             │
│  - Figma 同步缓存 (Redis)                            │
└─────────────────────────────────────────────────────┘
```

| 层级 | 技术选型 | 理由 |
|---|---|---|
| LLM | Claude Sonnet 4.5 (草稿) + CLIP (视觉) | 长文本生成 + 视觉相似度 |
| 搜索 | Elasticsearch + pgvector | 文本 + 视觉双检索 |
| 同步引擎 | Figma API + Style Dictionary | 行业标准,生态成熟 |
| 前端 | Next.js 14 + Radix UI | 设计师审美高,Tailwind 易定制 |
| 文件存储 | S3 + CDN | 设计源文件大,CDN 加速 |
| 部署 | Vercel + AWS S3 | 全球 CDN,设计师体验优先 |

## 5. Phase 0 资产盘点(1-2 周)

- [x] 盘点 `_DesignLib/` 10 个子目录已有设计方法论,提取"设计哲学/原则/案例"清单 → `docs/03-_DesignLib盘点_设计哲学清单_v0.1.md` v0.1(2026-08-25,24 条 DP)
- [x] 整理现有 Figma 文件结构,识别可入库的组件库/设计令牌/页面模板 → `docs/04-可入库资产清单_v0.1.md` v0.1(2026-08-31,4 类 32 组件 / 19 页面 / ~73 令牌 / 参考开放;无 Figma 权限,先出骨架,Phase 1 OAuth 后遍历)
- [ ] 与 Figma 团队确认 API 权限与文件范围,完成 OAuth 接入准备
- [x] **飞书 bot 雏形闭环**(2026-09-05):`bot/` 三模块(`webhook.py` FastAPI 路由 + `lark_client.py` lark-cli 包装 + `search_handler.py` 业务分发)实现"@bot 查设计资产 / 设计规范"基础查询,支持 `asset <关键词>` / `dp <关键词>` / `help` 三个命令(裸关键词默认走 asset),`api/main.py` 注册 `bot_router` + 后端 v0.3 → v0.4;`run_in_threadpool` 避免 uvicorn 单线程自调本地后端的死锁;`FEISHU_BOT_DRY_RUN=1` 默认 dry_run(只 print 不真发,Phase 0 试运行安全);5 项端到端验证通过(asset button → 5/133 命中 / dp 简约 → 1 命中 / help → 命令清单 / bot/health 200 / 裸关键词默认走 asset 5 命中);七前置全栈就绪(0828-0904 七 commit)差最后 20% 全部补齐,**Phase 0 业务工程 10/10 闭环**,剩余仅 Figma OAuth 真实入库(纯外部沟通) + 飞书 URL 验签 + 卡片化 + 限流(后续 T1-T5 增量)
- [x] **飞书 bot 增强 - 卡片化**(2026-09-06):新增 `bot/card.py` 飞书交互卡片渲染器(5 个函数:render_asset_card / render_dp_card / render_help_card / render_no_match_card / render_backend_down_card,飞书 interactive schema 完整,header + div × 3 + note + action 5 段式结构,header 模板色按命中形态区分 blue 命中 / purple DP / grey 0 命中 / red 后端未响应,底部"在 Web 看全部"action 按钮 URL 编码 `?q=` 直跳 Web `/assets` / `/dp` 详情);`bot/search_handler.py` `dispatch()` 增 `format: str = "text"` 参数(text 默认向后兼容 + format="card" 走 bot/card.py 渲染);5 项端到端验证通过(`bot/card` CLI 5 mode help/asset/dp/no_match/down 全 pass + text 模式向后兼容 2 项 + card 模式 help 7 elements 渲染正确 + card 模式 button 后端未启动降级为 red 模板 backend_down + 关键词缺失降级 help 卡片);`bot/` 从 547 行 → 约 850 行(+约 300 行 card 渲染);**业务工程清零后飞书 bot 试运行前的最后 1 公里第 1 步闭环**;下一刀候选:飞书 URL 验签(接 X-Lark-Signature 头 HMAC-SHA256 校验)+ 限流(token bucket 1 req/s)+ 关闭 dry_run 切真发(`FEISHU_BOT_DRY_RUN=0`)
- [x] **飞书 bot 增强 - URL 验签**(2026-09-08):新增 `bot/signature.py` 飞书 webhook URL 验签模块(`SignatureConfig` dataclass + `verify()` 纯函数 + `SignatureError` 异常,HMAC-SHA256 over `timestamp + nonce + secret + body`,base64 解码 X-Lark-Signature + hmac.compare_digest 防时序,默认 5min 时间戳容差防 replay,`from_env()` 读 `FEISHU_BOT_VERIFY_TOKEN` 未配则 `enabled=False` 静默跳过);`bot/webhook.py` 集成(读 raw body → 验签失败 `WebhookResponse(ok=False, note="signature failed: ...")` → 验签通过走原流程 + `note` 增 `sig=on/off` 标识,`HealthResponse` 增 `signature_enabled` 字段);`.env.example` 增 `FEISHU_BOT_VERIFY_TOKEN` + `FEISHU_BOT_TIMESTAMP_TOLERANCE`;`bot/__init__.py` v0.1.0 → v0.2.0;6/6 单元 + 3/3 FastAPI 集成验证通过(6 单元:env 关闭放行 + config 启用 + round-trip 通过 + 篡改 body mismatch + 过期 ts out of range + 缺 sig header missing;3 集成:health signature_enabled=True 显示 + 无签名 ok=False + 正确签名 ok=True 走业务分发);`bot/` 从 906 行 → 约 1066 行(+约 160 行);**业务工程清零后飞书 bot 试运行前的最后 1 公里第 2 步闭环**;下一刀候选:限流(token bucket 1 req/s,防刷)+ 关闭 dry_run 切真发(`FEISHU_BOT_DRY_RUN=0` + 真实 lark-cli `im +messages-send`)
- [x] **飞书 bot 增强 - 限流**(2026-09-09):新增 `bot/ratelimit.py` 飞书 webhook 限流模块(`RateLimitConfig` dataclass + `RateLimiter` 状态化 token bucket + 进程级单例 `get_default_limiter()`,每 chat_id 独立桶 + thread-safe,3 env 变量: `FEISHU_BOT_RATE_LIMIT_ENABLED` 默认 1 / `FEISHU_BOT_RATE_LIMIT_RPS` 默认 1.0 / `FEISHU_BOT_RATE_LIMIT_BURST` 默认 3);`bot/webhook.py` 集成(解析 chat_id 后立即 `limiter.acquire(chat_id)` → 超限返回 `WebhookResponse(ok=False, note="rate limited: retry after Xs ...")` 拒发,不放行业务分发,`HealthResponse` 增 4 字段 `rate_limit_enabled` / `rate_limit_rps` / `rate_limit_burst` / `rate_limit_active_chats`,`/health` note 增 "ratelimit=on(rps,burst,每 chat_id 独立 token bucket)" 标识);`.env.example` 增 3 行 env;`bot/__init__.py` v0.2.0 → v0.3.0;15/15 单元 + 3/3 FastAPI 集成验证通过(15 单元:env 默认值 + burst 3 连发全放行 + 第 4 拒 + 1s 后补满放行 + 不同 chat_id 桶独立 + enabled=False 永放行 + rps=0 关闭 + env 关闭开关 + 空 chat_id 不计入 + stats active_chat_ids + reset 单 chat + reset 全清 + 连发 3 拒后 0.5s/1s 补桶时间窗口 + 跨长间隔桶 cap 在 burst + 10 线程并发 1000 次 acquire 总放行=burst=10;3 集成:health v0.3.0 + 5 字段全显示 + 4 连发前 3 通 + 4-5 拒 + 2s 后放行 + 不同 chat 独立 + active_chats=2);`bot/` 从 1144 行 → 1340 行(+196 行,新 ratelimit.py 164 行 + webhook.py +29 行 + __init__.py +3 行);**飞书 bot 增强 3/3 卡片化 + URL 验签 + 限流全部闭环**,试运行前安全垫 3 件套齐了;**下一刀候选**:关闭 dry_run 切真发(`FEISHU_BOT_DRY_RUN=0` + 真实 lark-cli `im +messages-send`,前置必先完成限流)+ Phase 1 #1 Figma OAuth 真实入库(纯外部沟通)
- [x] **飞书 bot 增强 - 切真发 smoke 工具**(2026-09-10):新增 `bot/live_send.py` 切真发入口(`LiveSendConfig` dataclass + `probe_lark_cli()` 调 `lark-cli --profile <p> im +messages-send --help` 做无副作用鉴权校验 + `send_via_lark()` 先 probe 后 send 编排 + `ProbeError` 异常 + `from_env()` 工厂 + `__main__` CLI 调试入口,4 env 变量:`FEISHU_BOT_DRY_RUN` 默认 1 / `LARK_CLI_BIN` 默认 lark-cli / `LARK_PROFILE` 默认 design / `FEISHU_BOT_DEFAULT_CHAT_ID` 默认 None)+ `bot/test_live_send.py` 8 单元 + `bot/__init__.py` v0.3.0 → v0.4.0 增列 live_send 子模块 + README 新增"切真发部署清单"小节(5 步部署:配 lark-cli profile + 填 .env + probe + send_via_lark 单条真发 + 5 设计师 dogfood),目录结构段补 live_send.py / test_live_send.py;8/8 单元 + 3/3 端到端验证通过(8 单元:env 默认值 + env chat_id 覆盖 + dry_run=True 不调 subprocess + dry_run=False 调 subprocess 正确 + chat_id 缺失 raise ValueError + 显式 chat_id 覆盖 default + probe 调 lark-cli --help + probe 失败 raise ProbeError;3 端到端:probe 缺 lark-cli 抛 ProbeError + dry_run 模式 print 命令 probe_ok=False + chat_id 缺失退出码 2);`bot/` 从 1340 行 → ~1540 行(+200 行,新 live_send.py 165 行 + test_live_send.py 200 行 + __init__.py +3 行);**飞书 bot 增强 4/4 卡片化 + URL 验签 + 限流 + 切真发 smoke 工具全部闭环**,试运行前 4 件套全栈就绪;**下一刀候选**:5 设计师 dogfood 验收(关闭 dry_run 后首日 dogfood,纯外部沟通)+ Phase 1 #1 Figma OAuth 真实入库(纯外部沟通)
- [x] 起草设计资产元数据 schema(版本/作者/用途/状态/关联代码) → `docs/02-设计资产元数据-schema.md` v0.1(2026-08-24)
- [ ] 访谈 3 位核心设计师,验证"AI 草稿"需求的真实使用场景
- [x] 启动 Next.js 14 + FastAPI 最小骨架(Phase 1 前置) → `package.json` / `app/` / `api/main.py` / `requirements.txt` / `README.md` 工程章节就位(2026-08-27,v0.1)
- [x] 24 条 DP 真实数据从 `docs/03-哲学清单 v0.1` 抽到 `api/main.py` `_DP_CATALOG`,新增 `GET /api/v1/dp/search?q=` 关键词搜索端点(为 #4 飞书 bot 查规范提供后端数据)(2026-08-28,v0.2)
- [x] Web 端 `app/dp/page.tsx` 落 SSR 浏览页,支持 `?q=` 关键词搜索 + `?category=` 分类过滤,首页加知识库入口;Web 端先于飞书 bot 跑通规范查询 UI(2026-08-29,v0.3)
- [x] 4 类资产骨架清单(组件 32 / 页面 19 / 令牌 ~73 / 参考开放)对齐 `02-schema` v0.1,字段约定 §8 已写入 → `docs/04-可入库资产清单_v0.1.md` v0.1(2026-08-31,Phase 0 #2 闭环)
- [x] Phase 1 #1 资产库 MVP 切第一刀:后端 `api/assets.py` 8 件 fake-load 资产(4 类各 2 件),`GET /api/v1/assets` 列表 + `/summary` 计数 + `/{id}` 详情三端点就位,字段对齐 `02-schema` v0.1,后端版本 0.2 → 0.3(2026-09-01,Phase 1 启动)
- [x] Phase 1 #1 资产库 MVP 切第二刀:前端 `app/assets/page.tsx` SSR 列表页消费 8 件 fake-load,4 类计数卡(对齐 `docs/04` 命名空间预期) + `?kind=component|page|token|reference` + `?status=draft|in_review|approved|deprecated|archived` + `?category=` 三维过滤,首页加资产库入口卡,Web 端 0.3 → 0.4(2026-09-02,后端 → 前端"半成品接力"完成)
- [x] Phase 1 #1 资产库 MVP 切第三刀:32+19+73 件全量回填(命名空间批量生成脚本),`scripts/gen_assets.py` 从 `docs/04-可入库资产清单 v0.1` §2-§6 命名空间批量生成 125 件 stub(组件 30 / 页面 17 / 令牌 71 / 参考 7,跳过 6 件 manual fake-load 占用),`api/assets_stub.json` 落 JSON,`api/assets.py` 加载合并 → 8 manual + 125 stub = **133 件**;`/api/v1/assets` total 133 / `/summary` 命名空间 32-19-73-9(参考 9=首批 7+manual 2) 对齐 `docs/04` 命名空间目标,OAuth 接入后 stub 的 figma_ref / code_ref / purpose 字段批量回填真实值(2026-09-03,后端 v0.2 → v0.3,Phase 1 #1 推进 2/5 → 3/5)
- [x] Phase 1 #1 资产库 MVP 切第四刀:语义搜索端点 + 前端搜索框,`api/assets.py` 新增 `GET /api/v1/assets/search?q=&kind=&status=&limit=` 端点(133 件 + 字段权重 ranking:id 5x / tags 3x / 描述 2x / 子分类 1x + tokenize 中英文 + ?kind/?status 二次过滤收敛搜索池 + 路径顺序 /search 在 /{asset_id} 之前),`app/assets/page.tsx` 顶部加搜索框(支持 `?q=` URL 状态分享)+ 命中卡片显示 `★ score` 与 `命中字段` tooltip;5 测试查询(button 5 命中 / 登录 1 命中 / color 25 命中 / auth 2 命中 / inspiration 3 命中)+ 路由顺序(search / summary / {asset_id} 全部 HTTP 200)+ kind 二次过滤(q=button&kind=page→0 命中)端到端验证;Phase 2 升级预埋:CLIP 视觉相似度 + whoosh 倒排索引(2026-09-04,后端 v0.3 → v0.4 · Web 消费 v0.4 → v0.5,Phase 1 #1 推进 3/5 → **4/5**)
- [x] **Phase 0 #4 飞书 bot 雏形最后 20% 闭环**:七前置全栈就绪(0828-0904 七 commit)差飞书 webhook + lark-cli 调用层 + 飞书消息回复层最后 20% 全部补齐;新增 `bot/webhook.py` FastAPI 路由(`POST /api/v1/bot/webhook` 收飞书事件 / `GET /api/v1/bot/health` 健康检查)+ `bot/lark_client.py` lark-cli subprocess 包装(dry_run 默认开)+ `bot/search_handler.py` 业务分发(支持 `asset <关键词>` / `dp <关键词>` / `help` 三个命令,裸关键词默认走 asset)+ `api/main.py` 注册 `bot_router` + 后端 v0.3 → v0.4;关键技术决策:`run_in_threadpool` 把 search_handler 同步调用 + send_text subprocess 放到线程池,避免 uvicorn 单进程单线程事件循环自调本地后端的死锁;`urllib` 而非 `httpx` 同步调本机后端(本地高频,异步收益小);`FEISHU_BOT_DRY_RUN=1` 默认 dry_run(只 print 不真发,Phase 0 试运行安全,真发前显式 `FEISHU_BOT_DRY_RUN=0`);5 项端到端验证:asset button → 5/133 命中 / dp 简约 → 1 命中 / help → 命令清单 / `GET /api/v1/bot/health` 200 / 裸关键词 button → 默认走 asset 5 命中;**Phase 0 业务工程 10/10 闭环**(2026-09-05)

## 6. Phase 1 MVP(4-6 周)

- [ ] **#1 上线设计资产库**(支持组件/页面/令牌三层,语义搜索)
  - [x] 后端 fake-load 端点就位(2026-09-01,v0.3,8 件)
  - [x] 前端 `app/assets/page.tsx` 列表页消费 8 件(2026-09-02,v0.4,4 类计数卡 + 三维过滤)
  - [x] 32+19+73 件全量回填(2026-09-03,v0.3 → 8 manual + 125 stub = 133 件,`scripts/gen_assets.py` 命名空间批量生成)
  - [x] 语义搜索(2026-09-04,v0.4 后端 + v0.5 前端,关键词 + 字段权重 ranking · Phase 2 才上 CLIP)
  - [ ] Figma OAuth 接入后真实入库
- [ ] Figma 同步第一版(单向:资产入库)
- [ ] 上线设计评审协作 v1(上传→@人→评论→决策闭环)
- [ ] 上线 AI 草稿生成器 v1(基于 HTML+CSS,设计师可下载修改)
- [ ] 飞书 bot 查询接入(资产/规范/灵感)
- [ ] 内部 dogfood:5 个设计项目跑 4 周,采集使用数据

## 7. 里程碑

| Phase | 时长 | 关键产物 | 验证标准 |
|---|---|---|---|
| Phase 0 | 1-2 周 | 资产盘点 + Figma API 接入 + bot 雏形 | bot 命中规范查询准确率 ≥ 70% |
| Phase 1 (MVP) | 4-6 周 | 资产库 + 评审 + AI 草稿 | 设计师周活跃 ≥ 3 次,资产入库 ≥ 500 件 |
| Phase 2 (同步闭环) | 6-8 周 | 双向令牌同步 + 偏移告警 | 设计-代码一致率 ≥ 95% |
| Phase 3 (生态) | 8-12 周 | 外部设计社区接入 + 模板市场 | 10 个外部团队试用,留存 ≥ 30% |

## 8. 风险与下一步

**风险**:
- 设计师习惯 Figma 工作流,新平台需要明显价值才能迁移使用习惯
- 设计令牌双向同步偏移告警可能频繁,需要调阈值避免告警疲劳
- AI 草稿可能让设计师"懒得改直接用",反而稀释设计质量

**下一步**:
- 本周完成 Figma 文件结构盘点,出"可入库资产清单"
- 第 2 周与设计总监对齐"AI 草稿"定位(只做草稿,不做终稿)
- 飞书 bot 雏形下周一在设计群试运行
